<UserControl x:Class="newRBS.Views.SpectraPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:newRBS.Views"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding spectraPlotViewModel, Source={StaticResource Locator}}">
    <Grid>
        <oxy:Plot x:Name="Plot1" Title="A Graph" Model="{Binding plotModel}" Margin="10" Grid.Row="1">
        </oxy:Plot>
    </Grid>
</UserControl>
