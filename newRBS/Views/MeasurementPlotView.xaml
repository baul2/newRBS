<UserControl x:Class="newRBS.Views.SpectraPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:newRBS.Views"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:cmd="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Platform"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding measurementPlotViewModel, Source={StaticResource Locator}}">
    <DockPanel>
        <Border DockPanel.Dock="Top">
            <TextBlock TextAlignment="Center" Text="Hint: Use left button to mark, middle button to zoom and right button to pan."/>
        </Border>
        <oxy:PlotView Model="{Binding plotModel}" Margin="10"/>
    </DockPanel>
</UserControl>
